<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="/stylesheets/home_z/header.css">
    <link rel="stylesheet" href="/stylesheets/home_z/search.css">
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>

    <style>
      a {
          text-decoration: none; /* 显示下划线 */
          color: rgb(137, 187, 215);
      }
  </style>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const tabs = document.querySelectorAll('.tab');
            tabs.forEach(tab => {
                tab.addEventListener('click', function(event) {
                    // 获取与当前 a 标签关联的 radio 按钮
                    const targetRadioId = this.getAttribute('data-target');
                    const targetRadio = document.getElementById(targetRadioId);
                    
                    // 确保只有一个 radio 被选中
                    document.getElementsByName('tabs').forEach(radio => radio.checked = false);
                    targetRadio.checked = true;
                });
            });

            // 获取输入框元素
            const searchInput = document.getElementById('searchInput');

            // 添加键盘事件监听器
            searchInput.addEventListener('keydown', function(event) {
                // 检查是否按下了回车键（键码为13）
                if (event.key === 'Enter') {
                    // 获取输入框的值
                    const searchValue = searchInput.value;

                    // 执行跳转，并将输入框的值作为查询参数传递
                    console.log(`Redirecting to /search?search=${encodeURIComponent(searchValue)}`);
                    location.href = '/search';
                }
            });
        });
    </script>
</head>
<body>
<div class="container">
    <div class="tabs">
      <input type="radio" id="radio-0" name="tabs" checked />
      <a class="tab" data-target="radio-0" href="/content" target="content">首页</a>
      <input type="radio" id="radio-1" name="tabs" />
      <a class="tab" data-target="radio-1" href="/all_game" target="content">全部游戏</a>
      <input type="radio" id="radio-2" name="tabs" />
      <a class="tab" data-target="radio-2" href="/shopping_cart" target="content">愿望单</a>
      <input type="radio" id="radio-3" name="tabs" />
      <a class="tab" data-target="radio-3" href="/order_set_z" target="content">全部订单</a>
      <input type="radio" id="radio-4" name="tabs" />
      <a class="tab" data-target="radio-4" href="/wallet" target="content">我的钱包</a>
      <input type="radio" id="radio-5" name="tabs" />
      <a class="tab" data-target="radio-5" href="/about_us" target="content">关于我们</a>
        <div class="flexbox">
          <form action="/search" method="get">
            <div class="search">
              <div>
                 <input type="text" id="searchInput" name="search" placeholder="Search . . ." required>
                 <button type="submit" id="Button" name="tabButton"></button>
                <!-- <button type="button" id="Button" name="tabButton" onclick="location.href='/search';" data-target="Button">点击我前往游戏分类</button> -->
              </div>
            </div>
          </form>
        </div>
        <span class="glider"></span>
    </div>
</div>
</body>
</html>