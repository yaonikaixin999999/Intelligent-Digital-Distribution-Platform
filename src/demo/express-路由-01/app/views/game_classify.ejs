<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #ffffff;
            color: #1b2838;
            margin: 0;
            padding: 0;
        }
        
        .flex-container {
            display: flex;
            height: 100vh;
            overflow: hidden;
        }
        
        iframe {
            border: none;
            width: 100%;
            height: 100%;
        }
        
        .left-frame {
            background-color: #f5f5f5;
            border-right: 1px solid #dcdcdc;
            flex: 8;
        }
        
        .right-frame {
            background-color: #e6eef9;
            flex: 1;
            margin-top : 0px;
            align-self: flex-start; /* 将右侧iframe顶到最上面 */
        }
    </style>
</head>

<body>
    <div class="flex-container">
        <iframe src="/classify_left" name="left" class="left-frame"></iframe>
        <iframe src="/classify_right" name="right" class="right-frame"></iframe>
    </div>
</body>
</html>